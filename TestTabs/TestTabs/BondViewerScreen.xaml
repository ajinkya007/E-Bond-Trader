<UserControl x:Name="BondViewer" x:Class="TestTabs.BondViewerScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestTabs"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Foreground="#FFF5F1F1">
    <UserControl.Background>
        <ImageBrush ImageSource="BondViewerBackground.jpg"/>
    </UserControl.Background>
    <ScrollViewer>
        <Grid>

            <DataGrid x:Name="BondsDataGrid" HorizontalAlignment="Stretch" Height="155" Margin="0,108,0,0" VerticalAlignment="Top" Width="Auto" MouseDoubleClick="BondsDataGrid_MouseDoubleClick" BorderBrush="{x:Null}" Foreground="Black" >
                <DataGrid.Background>
                    <SolidColorBrush Color="#FFF0F0F0" Opacity="0"/>
                </DataGrid.Background>
            </DataGrid>
            <Label x:Name="BondViewerHeading" Content="Bond Viewer" HorizontalAlignment="Left" Height="48" Margin="10,34,0,0" VerticalAlignment="Top" Width="195" FontSize="26.667" FontFamily="Caladea" FontWeight="Bold" Foreground="#FF161711" Background="{x:Null}"/>

        </Grid>
    </ScrollViewer>
</UserControl>
